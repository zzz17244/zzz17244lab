const product = {
    productName : "Computer",
    price : 12000,
    stock :10
}
const {productName,stock} = product
console.log(productName)
console.log(stock)
// concept is the match name in object with name in new variables tobe used must be match name if we use this format