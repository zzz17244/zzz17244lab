// Array MAP
// objective is to map item in array and perform operaton of cmodify item in array

const numbers = [10,20,30,40]

const result =  numbers.map( item => {
         new_item   = item * 2
         return new_item 
})
// shot format
const result1 = numbers.map(e=>e*20)
console.log(numbers)
console.log(result)
console.log(result1)

